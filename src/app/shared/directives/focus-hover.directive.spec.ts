import { FocusHoverDirective } from './focus-hover.directive';

describe('FocusHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusHoverDirective();
    expect(directive).toBeTruthy();
  });
});
